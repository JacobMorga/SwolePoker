from config import *

class Agent:
    def __init__(self, obs_size, action_size): 
    def select_action(self, obs):        # Epsilon-greedy, softmax, etc.
    def learn(self):                     # Gradient update step
    def save(self, path): 
    def load(self, path): 